export const metadata = {
  title: 'Leterizza Studio',
  description: 'Painel administrativo da Leterizza',
}

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
